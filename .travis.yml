sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t moshegelberg/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true moshegelberg/docker-react npm run test
